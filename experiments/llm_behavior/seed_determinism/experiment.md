---
title: "EXP-05 — Seed + T=0 Determinism"
tags: [experiment, llm-behavior, temperature, seed, determinism, reproducibility, openai]
aliases: [exp-05, seed-determinism, temperature-seed]
---

# EXP-05 — Seed + T=0 Determinism

**Raised by:** [RQ-01](../../../research/questions/open_questions.md#rq-01--temperature-seed-determinism)
**Category:** `llm_behavior`
**Date:** 2026-03-04
**Status:** ✅ Complete

---

## Hypothesis

> `seed=42` combined with `T=0` reduces but does not eliminate output variation.
> `system_fingerprint` changes (backend model updates) will break reproducibility even with a fixed seed.
> Without seed, T=0 will show significant output variation (~same as EXP-01 baseline).

**Null hypothesis:** There is no statistically significant difference in output identity rate between `T=0+seed` and `T=0 (no seed)`.

---

## Background

EXP-01 ran 20 determinism checks at `T=0` and found that all returned `DIFFERS`.
This contradicts the theoretical expectation that temperature=0 forces greedy decoding.

OpenAI's documentation for the `seed` parameter states:
> *"If specified, our system will make a best effort to sample deterministically...
> Determinism is not guaranteed, and you should refer to the `system_fingerprint`
> response parameter to monitor changes in the backend."*

The phrase "best effort / high probability" means seed is a probabilistic guarantee — not an absolute one.
This experiment quantifies exactly how probabilistic it is.

---

## Setup

| Parameter | Value |
|-----------|-------|
| Model | `gpt-4o-mini` |
| Temperature | `0.0` (fixed throughout) |
| Seed (treatment) | `42` |
| Seed (control) | None |
| Calls per condition | 30 |
| Prompts tested | 3 (analytical, creative, code) |
| Total API calls | ~180 |

**Variable table:**

| Variable | Treatment Group | Control Group |
|----------|----------------|---------------|
| `seed` | `42` | omitted |
| `temperature` | `0.0` | `0.0` |
| `max_tokens` | `300` | `300` |
| `system_fingerprint` logged | Yes | Yes |

**Success criteria:**
- [ ] Seed group shows statistically higher identity rate than no-seed group
- [ ] `system_fingerprint` is logged and constant or change is detected
- [ ] Minimum 30 calls per condition per prompt type collected
- [ ] Results written to `results.md` automatically

---

## Code

See [`code.py`](code.py) — runs from project root:

```bash
python experiments/llm_behavior/seed_determinism/code.py
```

Outputs:
- Console report with identity rate per condition and prompt
- `results.md` auto-generated in this folder

---

## Results

See [`results.md`](results.md) — auto-generated by `code.py`.

---

## Analysis

See [`analysis.md`](analysis.md).

---

## Key Insight

**`seed` has zero effect at `T=0`** — identity rate delta was +0.0% across all prompt types.
The remaining non-determinism at T=0 is **floating-point arithmetic noise** (not random sampling),
which `seed` cannot fix. Code outputs were 100% identical in *both* conditions — driven by
task over-constraint, not seed. A `system_fingerprint` change was observed mid-experiment
in the no-seed code condition but did not affect output.

**The only reliable reproducibility strategy is application-layer response caching** (hash input → store output).

---

## Next Research Question

- **RQ-08:** Does T=0 + seed produce consistent tool-call selection in agents — or does the same variation pattern appear in structured output (function names, arguments)?
- **RQ-09:** If seed does not guarantee determinism, what is the correct production architecture for reproducible LLM outputs — response hashing, external cache, or deterministic post-processing?

---

## Links

**Concepts:**
- [`Seed Determinism`](../../../research/concepts/seed_determinism.md) — primary concept note for this experiment
- [`LLM Behavior`](../../../research/concepts/llm_behavior.md) — parent concept: sampling, control surfaces, reproducibility

**Research Questions answered:**
- [RQ-01](../../../research/questions/open_questions.md#rq-01--temperature-seed-determinism) — Does `seed` combined with `T=0` guarantee byte-exact identical outputs? → **No. Δ=+0.0% across all prompt types.**

**Research Questions raised:**
- [RQ-08](../../../research/questions/open_questions.md#rq-08--agent-temperature-reliability) — Does T=0 + seed produce consistent tool-call selection in agents?
