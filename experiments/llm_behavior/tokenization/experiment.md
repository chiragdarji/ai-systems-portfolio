---
title: "Experiment 06 — Tokenization: Domain-Specific Token Ratios"
tags: [experiment, tokenization, tiktoken, bpe, llm-behavior, cost, context-window]
aliases: [tokenization-experiment, exp-06, bpe-experiment]
---

# Experiment 06 — Tokenization: Domain-Specific Token Ratios

**ID:** EXP-06
**Phase:** LLM Behavior
**Category folder:** `experiments/llm_behavior/tokenization/`
**Status:** 📋 Planned
**Model:** No API calls — `tiktoken` only (`cl100k_base` vocabulary)
**Estimated cost:** $0.00 (local computation only)

---

## Hypothesis

> *"If text is drawn from a domain where vocabulary coverage is low — code identifiers, non-Latin scripts, medical terminology, or structured data — then the token-per-word ratio will be measurably higher (≥ 1.5×) than common English prose (1.0–1.3 tokens/word), because the BPE algorithm must decompose unfamiliar character sequences into shorter subword units."*

**Prediction:** Token-per-word ratio varies by at least 2× between the lowest-cost domain (common English prose) and the highest-cost domain (non-Latin script or emoji).

**Because:** BPE vocabulary is trained on web-scale English text. Tokens for rare subwords, code identifiers, structured-data symbols, and non-Latin characters are absent or fragmented in the vocabulary — forcing the tokenizer to fall back to shorter units or individual bytes.

---

## Background

**Concept foundation:**
- [`research/concepts/tokenization.md`](../../../research/concepts/tokenization.md) — BPE algorithm, tiktoken usage, per-domain ratios

**Prior experiments this builds on:**
- [EXP-03 — Token Limits & Truncation](../token_limit/experiment.md) — established that `max_tokens` is counted in tokens and that `finish_reason="length"` is domain-sensitive; but word count was never compared to token count
- [EXP-01 — Temperature](../temperature/experiment.md) — `max_tokens=400` caused mid-token truncation, observed without measuring what that token count corresponded to in words

**Open question being addressed:**
> Does token count per word vary predictably by domain, or is the conventional "1,000 words ≈ 750 tokens" rule reliable enough for production cost estimates?

---

## Setup

### Variables

| Type | Variable | Values / Range |
|---|---|---|
| **Independent** | Input domain | 8 domains: common English, technical English, Python code, JSON, Japanese, Arabic, medical/legal, emoji |
| **Independent** | Text length | Short (50 words), Medium (200 words), Long (500 words) — to test if ratio is stable at different lengths |
| **Controlled** | Tokenizer | `cl100k_base` (GPT-4 / GPT-4o vocabulary, 100,277 tokens) |
| **Controlled** | Encoding function | `tiktoken.get_encoding("cl100k_base").encode()` |
| **Measured** | Token count | `len(enc.encode(text))` |
| **Measured** | Word count | `len(text.split())` |
| **Measured** | Token-per-word ratio | `token_count / word_count` |
| **Measured** | Token boundary visualization | Per-token colour-coded output string |

### Domains Tested

| # | Domain | Representative text | Expected ratio |
|---|--------|---------------------|:--------------:|
| 1 | Common English prose | News article excerpt | 1.0 – 1.3 |
| 2 | Technical English (AI paper) | Abstract from arXiv | 1.3 – 1.7 |
| 3 | Python source code | Function with docstring | 1.5 – 2.0 |
| 4 | JSON / structured data | Nested JSON object | 1.8 – 2.5 |
| 5 | Japanese | Short paragraph (hiragana/kanji) | 1.5 – 3.0 |
| 6 | Arabic | Short paragraph | 2.0 – 5.0 |
| 7 | Medical / legal terminology | Clinical note excerpt | 1.5 – 2.5 |
| 8 | Emoji-heavy text | Social media style | 2.0 – 6.0 per emoji |

### Success Criteria

- [ ] Hypothesis clearly confirmed or refuted with measured ratios per domain
- [ ] `code.py` runs with no API key — only `tiktoken` required
- [ ] Token boundaries are visualized as labelled segments in output
- [ ] `results.md` auto-generated by `code.py`
- [ ] Ratio comparison table produced with `pip install tiktoken` as the only dependency
- [ ] Key insight captured as a one-sentence production rule

---

## Code

**Install dependency:**
```bash
pip install tiktoken
```

**Run:**
```bash
python experiments/llm_behavior/tokenization/code.py
```

**Output files generated:**
- `results.md` — auto-written by the script on every run; contains all ratio tables and boundary examples

**Script structure:**
```
SAMPLE_TEXTS        → dict of domain → text sample
analyze_domain()    → encode text, count tokens/words, build boundary display
run_experiment()    → iterate all domains × lengths, collect metrics
print_report()      → formatted console output with aligned table
write_markdown()    → writes results.md with all tables and examples
main()              → orchestrates all four
```

> ⚠ Write `experiment.md` fully before starting `code.py`.

---

## Results

> This section is populated after running `code.py`.
> Replace placeholders with real numbers from `results.md`.

**Run date:** —
**Total API calls:** 0 (tiktoken is local)
**Estimated cost:** $0.00

### Domain Ratio Summary Table

| Domain | Words | Tokens | Ratio | vs English prose |
|--------|------:|-------:|------:|:----------------:|
| Common English prose | — | — | — | baseline |
| Technical English | — | — | — | — |
| Python code | — | — | — | — |
| JSON / structured data | — | — | — | — |
| Japanese | — | — | — | — |
| Arabic | — | — | — | — |
| Medical / legal | — | — | — | — |
| Emoji-heavy | — | — | — | — |

### Unexpected Findings

- *(document anything that surprised you — even negative results)*

---

## Analysis

> Write after reviewing results. Cite specific numbers.
> Do not write this section before `results.md` exists.

### What the data shows

*(2–3 paragraphs of evidence-based interpretation)*

### Hypothesis verdict

- [ ] **Confirmed** — because ...
- [ ] **Partially confirmed** — confirmed for X but not Y because ...
- [ ] **Refuted** — because ...

### Comparison to prior experiments

| Finding here | Finding in prior EXP | Relationship |
|---|---|---|
| *(token-per-word ratio for code)* | EXP-03: code generation required 800+ tokens | *(extends — explains the mechanism)* |
| *(ratio stability across text length)* | EXP-03: RAG stopped at 51 tokens regardless of budget | *(confirms natural stop independent of max_tokens)* |

---

## Key Insight

> Distil the experiment into 1–3 numbered claims.
> Each must be: specific, falsifiable, backed by a number from results.

1. *(fill after run)*
2. *(fill after run)*
3. *(fill after run)*

---

## Next Research Question

**Question:** *Does using a domain-specific tokenizer (e.g. a medical or code-specific vocabulary trained with BPE on domain corpora) reduce the token-per-word ratio by a measurable amount — reducing cost and context pressure for specialised applications?*

**Why it matters:** If domain specialisation reduces token count by 30–40%, a domain-specific model fine-tuned with its own tokenizer would be meaningfully cheaper to operate than a general-purpose model on the same domain text — a key architectural trade-off in production AI systems.

**Suggested next experiment:** EXP-06b — `experiments/llm_behavior/tokenization_domains/` — compare `cl100k_base` vs `o200k_base` token counts on identical domain texts

---

## Links

**Concepts:**
- [`Tokenization`](../../../research/concepts/tokenization.md) — BPE, tiktoken, domain token ratios
- [`LLM Behavior`](../../../research/concepts/llm_behavior.md) — token budget, cost, context window

**Prior experiments:**
- [EXP-03 — Token Limit](../token_limit/experiment.md) — established `max_tokens` behavior; this experiment explains the token count mechanism

---

*Register this experiment in [`experiments/EXPERIMENT_REGISTRY.md`](../../EXPERIMENT_REGISTRY.md) before starting.*
*Update [`experiments/EXPERIMENT_DASHBOARD.md`](../../EXPERIMENT_DASHBOARD.md) with the new entry.*
